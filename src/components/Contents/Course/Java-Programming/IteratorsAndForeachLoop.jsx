// IteratorsAndForeachLoop.jsx
import React from 'react';

const IteratorsAndForeachLoop = () => (
  <div className="section">
    <h2 className="heading">Iterators and foreach Loop</h2>
    <p className="paragraph">
      Iterators and the foreach loop are used to iterate over collections. They simplify the process of accessing and processing elements.
    </p>
  </div>
);

export default IteratorsAndForeachLoop;
